---
name: Sys_OsRelease_Lite
# yamllint disable rule:line-length
on:  # yamllint disable-line rule:truthy
  push:
    paths: ['lite/**', '.github/workflows/test-lite.yml']
  pull_request:
    paths: ['lite/**', '.github/workflows/test-lite.yml']
  workflow_dispatch:
  # schedule:
  #   - cron: '42 5 * * *'
defaults:
  run:
    working-directory: ./lite
jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        runner: [ubuntu-latest, macos-latest, windows-latest]
        perl: ['5.10', '5.20', '5.30', '5.36', '5.42']
        exclude:
          - runner: windows-latest
            perl: '5.10'
          - runner: windows-latest
            perl: '5.20'
    runs-on: ${{matrix.runner}}
    name: OS ${{matrix.runner}} Perl ${{matrix.perl}}
    steps:
      - uses: actions/checkout@v6
      - name: Set up perl
        uses: shogo82148/actions-setup-perl@v1
        with:
          perl-version: ${{ matrix.perl }}
          distribution: ${{ ( startsWith( matrix.runner, 'windows-' ) && 'strawberry' ) || 'default' }}
      - name: Show Perl Version
        run: |
          perl -v
      - name: Install ExtUtils::MakeMaker + build deps
        run: |
          cpanm -v
          cpanm --dev --notest ExtUtils::MakeMaker~'>= 7.65_02'  # use dev release with fix for spurious errors
          cpanm --notest Readonly CPAN::Changes Versioning::Scheme::Semantic
      - name: Install Modules
        run: |
          perl Makefile.PL
          cpanm --installdeps .
      - name: Show Errors on Windows
        if: ${{ failure() && startsWith( matrix.runner, 'windows-')}}
        run: |
           ls -l C:/Users/
           ls -l C:/Users/RUNNER~1/
           cat C:/Users/runneradmin/.cpanm/work/*/build.log
      - name: Show Errors on Ubuntu
        if: ${{ failure() && startsWith( matrix.runner, 'ubuntu-')}}
        run: |
           cat /home/runner/.cpanm/work/*/build.log
      - name: Show Errors on OSX
        if: ${{ failure() && startsWith( matrix.runner, 'macos-')}}
        run: |
           cat  /Users/runner/.cpanm/work/*/build.log
      - name: Run tests
        run: |
          make test
